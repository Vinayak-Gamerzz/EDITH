fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
websockets>=12,<14
apscheduler>=3.10,<4.0
playwright>=1.45,<2.0
reportlab>=4.0,<5.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
matplotlib>=3.8,<4.0
pillow>=10.0,<11.0
pypdf>=4.0,<7.0
python-pptx>=1.0.0
pytest>=8.0
beautifulsoup4>=4.12,<5.0
cryptography>=42.0,<45.0
opencv-python-headless>=4.8,<5.0
numpy>=1.26,<3.0
# Voice: STT via Groq Whisper (default), TTS via Edge neural voices (default).
# Both are imported lazily; Zenith degrades gracefully if they're absent.
groq>=0.10,<1.0
edge-tts>=6.1,<8.0
gTTS>=2.4,<4.0        # TTS fallback when Edge is unavailable
SpeechRecognition>=3.10,<4.0  # STT fallback when Groq key is absent
# YouTube transcript (captions) for summarization. yt-dlp is the fallback when
# youtube_transcript_api has no captions; both are lazy-imported.
youtube-transcript-api>=1.0,<2.0
yt-dlp>=2025.1.0